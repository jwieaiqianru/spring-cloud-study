spring:
  profiles:
    active: local
  application:
    name: eureka-client-peer1
    #web 容器的端口号
server:
  port: 8080


management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    shutdown:
      enabled: true
    # 监视端口号
  server:
    port: 9001

